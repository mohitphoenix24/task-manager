DATABASE_URL="postgresql://user:password@localhost:5432/taskmanager"
JWT_SECRET="replace-with-a-long-random-string"
PORT=4000
# Leave unset in dev to allow all origins. Set to the frontend's exact origin in production.
ALLOWED_ORIGIN=
